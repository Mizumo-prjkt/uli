# UI Directory for the Compositor

Please remember:

When making a new UI Window or group, create a new dir and contain the code in there

Example:

Main Menu should be in

`./ui/Main_Menu`

where 

`Main_Menu.cpp` and `Main_Menu.hpp` is located

Language is located at `compositor/lib/components/language` and thats where the translations should be kept